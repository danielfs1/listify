# listify

Downloading dependecies
----------------------
cd into 'client'
run 'bower install'
cd into 'server'
run 'npm install'

Running
---------
cd into 'client'
run 'grunt serve' (close the window that opens)

cd into 'server'
run 'npm test'
